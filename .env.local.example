# Use quando a API rodar NA SUA MÁQUINA (npm run start:dev)
# com Postgres, Redis e RabbitMQ no Docker.
# Copie para .env.local:  copy .env.local.example .env.local

# Database (localhost = porta exposta pelo container postgres)
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=admin
DB_PASSWORD=admin_password
DB_DATABASE=cinema_db

# Redis
REDIS_HOST=localhost
REDIS_PORT=6379

# RabbitMQ
RABBITMQ_USER=guest
RABBITMQ_PASS=guest
RABBITMQ_HOST=localhost
RABBITMQ_PORT=5672

# App
PORT=3000
NODE_ENV=development
